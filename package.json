{
  "name": "landingai-js",
  "version": "1.0.0",
  "description": "Landing AI JS libraries",
  "main": "index.js",
  "repository": "git@github.com:landing-ai/landingai-js.git",
  "author": "Lan Tian <tian.lan@landing.ai>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "tsc": "npx lerna run tsc",
    "publish": "npx lerna publish --canary --dist-tag latest",
    "build-publish": "yarn build && yarn tsc && yarn publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2",
    "typescript": "^5.0.4"
  }
}
