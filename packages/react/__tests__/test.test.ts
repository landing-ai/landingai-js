import core, { palette, testString } from '@landingai-js/core';

it('test importing core package', () => {
  console.log('palette', palette);
  console.log('testString', testString);
  console.log('core', core);
});